import { Text } from "react-native";
import CustomCard from "../../../../layouts/CustomCard";

const BrandListItem = ({ index, length }) => {
  return <CustomCard index={index} length={length}></CustomCard>;
};

export default BrandListItem;
